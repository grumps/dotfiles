[alias]
    root = rev-parse --show-toplevel
    graph = log --graph --branches --remotes --tags  --format=format:'%Cgreen%h %Creset• %<(60,trunc)%s (%cN, %cr) %Cred%d' --date-order
[user]
	name = Max Resnick
    email = max@ofmax.li
[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    ui = true
    pager = true
#[url "git@priv.git:"]
#	insteadOf = https://git.ofmax.li
[commit]
	gpgSign = true
[core]
	hookspath = ~/projects/git-hooks/hooks
    pager = delta
[interactive]
    # diffFilter = delta
[push]
	default = current
[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[delta]
    # features = side-by-side line-numbers decorations
    features = line-numbers decorations
    syntax-theme = gruvbox-dark
    # plus-style = syntax "#003800"
    # minus-style = syntax "#3f0001"
    true-color = always

[delta "decorations"]
    commit-decoration-style = bold yellow box ul
    file-style = bold yellow ul
    file-decoration-style = none
    hunk-header-decoration-style = cyan box ul

#[delta "line-numbers"]
#    line-numbers-left-style = cyan
#    line-numbers-right-style = cyan
#    line-numbers-minus-style = 124
#    line-numbers-plus-style = 28
#
[credential "https://git.ofmax.li"]
	helper = gopass
    username = grumps
[credential "https://git.ofmax.li/mgmt"]
	helper = gopass
    username = admin
[http]
	postBuffer = 524288000
